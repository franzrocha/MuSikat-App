import 'package:flutter/material.dart';

class FERScreen extends StatelessWidget {
  const FERScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
